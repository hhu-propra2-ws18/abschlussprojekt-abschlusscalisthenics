version: "3.1"

services:
  database:
      image: mysql:5.7
      #ports:
        #- "3306:3306"
      volumes:
        - ./data:/var/lib/mysql:rw
      environment:
        - "MYSQL_ROOT_PASSWORD=password"
        - "MYSQL_DATABASE=leihordiedb"
        - "MYSQL_USER=nutzer"
        - "MYSQL_PASSWORD=1234"

  propay:
      image: propra/propay
      ports:
        - "8888:8888"
      # Optional
      volumes:
        - ./data:/code/data